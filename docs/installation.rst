============
Installation
============

At the command line::

    $ easy_install django-mdash

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mdash
    $ pip install django-mdash
